// !This is a userdefined wrapper react has something called fragments which is used to render
// This is just to show that we can make and use user-defined wrappers.

const Wrapper = (props) => {
  return props.children;
};
export default Wrapper;
